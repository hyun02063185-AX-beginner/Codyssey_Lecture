# 검증 리포트 — AX 상자1 (boundary-keeper)

## 대상 및 방법
- 코드 diff 없음 — 콘텐츠 소급 감사로 전환.
- 대조 문서: `C:\Users\hyun0\AX_Lecture\docs\엔진_경계.md`(§1~§7).
- 조사 파일: `C:\Users\hyun0\AX_Lecture\js\data.js`(`CURRICULUM.boxes[0]`, `id:"why"`, 01~05강, L19~308), `assets/diagram-ax-stairs.svg`·`diagram-fallen-barriers.svg`·`diagram-next-word.svg`·`diagram-prompt-4.svg`, 그리고 `js/room.js`·`js/main.js`·`js/slides.js`·`js/admin.js`(engine 소비 지점), `tests/content-lint.test.js`.
- 자매 저장소: `C:\Users\hyun0\AI_First_Step`의 `docs/엔진_경계.md`·`docs/UX_원칙.md`·`docs/톤앤매너.md`.

---

## ① 상자1(why) 자산 — 분류 일치성 점검

| 항목 | 분류(엔진_경계.md 기준) | 확인 결과 |
|---|---|---|
| `js/data.js` 파일 전체 | §2 사이트 고유 ("전량 교체 대상") | 분류 자체는 맞음. 단, 아래 세부 발견 참고 |
| `CURRICULUM.boxes[].id/name/theme/accent` 키 이름 | §2 산하이나 실질적으로 엔진 소비 | **아래 발견 1 참고** |
| `lectures[].slides[].type` 값(`cover/big/bullets/quote/split/image/closing`) | §4 예외(`tests/content-lint.test.js`의 `VALID_SLIDE_TYPES`) | 상자1 7종 모두 이 집합 안에 있음 — **일치, 문제없음** |
| `image` 슬라이드의 `src: 'assets/diagram-*.svg'` 4개 | §2 사이트 고유(`assets/*`) | 4개 파일(`diagram-ax-stairs.svg`·`diagram-fallen-barriers.svg`·`diagram-next-word.svg`·`diagram-prompt-4.svg`) 모두 `assets/`에 실존 확인. 분류·존재 모두 정상 |
| 슬라이드 `kicker` 필드의 `"상자 1 · 왜 · 01"` 등 리터럴 | 미분류(신규) | **아래 발견 2 참고** |
| `box.accent`(`"#22d3ee"` 등) | §2 산하이나 색상값이라는 점에서 스킨 개념과 인접 | **아래 발견 3 참고(관찰)** |

### 발견 1 — `CURRICULUM.boxes` 최상위 스키마는 사실상 엔진과의 암묵적 계약
`box.id/name/theme/accent`, `lectures[].id/title/tagline`은 `js/room.js`(L22-31, L66-67, L79), `js/main.js`(L61, L165-166, L249-251, L489-491), `js/slides.js`(L25, L158, L345), `js/admin.js`(L135, L140)에서 직접 소비된다. 즉 §2가 "data.js는 전량 교체 대상"이라고만 적어놓았지만, 실제로는 **키 이름·형태(shape)는 엔진 계약이고 값만 교체 대상**이다. `tests/content-lint.test.js`가 `LECTURE_COUNT`/`VALID_SLIDE_TYPES`로 일부 이 계약을 검증하고 있어(§4 예외 목록에 이미 등재) 실질적 안전판은 있으나, `엔진_경계.md` §2 본문 자체엔 이 뉘앙스가 명시돼 있지 않다.
- **심각도**: 낮음~중간(문서 정확성 문제, 코드 동작엔 영향 없음)
- **수용 가능 여부**: 수용 가능 — 즉시 조치 불요. 다만 §2 또는 §3에 "data.js 최상위 스키마(box.*, lectures[].*, slides[].type)는 값 교체 대상이지 구조 변경 대상이 아니다"라는 한 줄 보강을 권장.

### 발견 2 — `kicker`에 "상자" 텍스트가 하드코딩됨 (신규 애매 항목 후보)
`js/room.js:67`은 이미 `SITE_CONFIG.boxLabel || "상자"` fallback으로 백포트돼 있는데(§7 2026-07-21 이력), 상자1 5개 강 전부의 `kicker` 필드는 `'상자 1 · 왜 · 01'`처럼 "상자"라는 단어를 **슬라이드 콘텐츠 문자열 안에 직접** 박아뒀다(L30, L88, L139, L198, L256). 복제 사이트가 `boxLabel`을 바꾸면 `room.js`의 UI 라벨은 바뀌지만, 슬라이드 내부 kicker 텍스트는 `data.js` 콘텐츠를 강별로 일일이 다시 써야만 바뀐다 — `엔진_경계.md` §3의 "입장 화면 안내문" 항목(문장의 색깔은 사이트 고유, "상자" 메타포는 엔진 개념)과 같은 성격의 갈라짐 지점인데 아직 §3에 명문화돼 있지 않다.
- **심각도**: 낮음
- **수용 가능 여부**: 수용 가능 — `data.js`는 어차피 사이트별 전량 교체 대상이라 복제 시 자연히 다시 쓰게 되므로 당장 문제는 아니다. 다만 §3에 "신규 애매 항목"으로 한 줄 등재를 제안(아래 이력 문안 참고).

### 발견 3 — `box.accent`는 CSS 스킨 값과는 별개의 "사이트 고유 색상 축" (관찰, 위반 아님)
`box.accent`(예: `#22d3ee`)는 `css/style.css`의 스킨 값 블록(`data-skin`)과 무관하게 `--box-accent`/`--card-accent`/`--toc-accent` CSS 변수로 소비된다(room.js/main.js). §2에 이미 `data.js`·`css 스킨 값 블록`이 각각 등재돼 있어 분류상 모순은 없지만, "사이트 고유 색"이 두 갈래(스킨 팔레트 vs 상자별 accent)로 존재한다는 점이 문서에 드러나 있지 않다.
- **심각도**: 낮음(정보성)
- **수용 가능 여부**: 수용 가능 — 조치 불요, 참고 사항으로만 기록.

### 관찰(경계 위반 아님) — `assets/` 파일명 컨벤션 혼재
상자1이 쓰는 4개 SVG는 모두 `diagram-영문설명.svg` 패턴인데, 폴더 전체에는 `6강_생성형지형도.svg`·`7강_RAG_구조도.svg` 같은 `N강_한글이름.svg` 패턴이 섞여 있다. `assets/*`는 §2 완전 사이트 고유 영역이라 엔진 경계 위반은 아니며, 상자1 자체는 일관돼 있다.
- **심각도**: 낮음(정보성) / **수용 가능**: 예, 경계 이슈 아님.

**경계 침범**: 상자1 조사에서 공유 엔진 파일(§1)에 콘텐츠성 변경이 섞여 들어간 사례는 발견되지 않음 — **침범 없음**.

---

## ② 규범 문서 동기화 상태 — 본진 ↔ AI_First_Step

| 문서 | 결과 | 판정 |
|---|---|---|
| `docs/UX_원칙.md` | 두 저장소 **바이트 단위 완전 동일**(diff 0) | 동기화 확인됨 |
| `docs/엔진_경계.md` | §1~§6 본문은 자기지시적 문구("이 저장소" vs "AI_First_Step 저장소")만 다르고 내용은 동일. **§7 이식 이력에서 실질적 차이 발견** | 아래 발견 4·5 참고 |
| `docs/톤앤매너.md` | 본진에만 "기본판/경험판 규격 분리"(L37-38, 7·8강 `slidesPersonal` 관련) 조항 존재, AI_First_Step엔 없음 | **§4 예외 대상 — 다르다고 위반 아님**(콘텐츠가 실제로 달라서 발생하는 §4 명시적 예외) |

### 발견 4 — AI_First_Step의 `엔진_경계.md` §7에 "SITE_CONFIG fallback 4건" 백포트 기록이 없음
본진 `docs/엔진_경계.md` §7 첫 항목("2026-07-22 — AI_First_Step → 본진, 1차 (4건)", 실제 커밋 로그상 `927d7ce`·`e4278ef` 근방에서 확인되며 문서 내 날짜 표기는 2026-07-21)은 `main.js`/`room.js`/`intro.js`의 SITE_CONFIG fallback 3건 + `css/style.css`의 `.pdoor` `[hidden]` 가드 버그픽스 1건을 상세히 기록하고 있다. 이 4건은 확인 결과 실제로 반영돼 있다(`js/room.js:67`의 `SITE_CONFIG.boxLabel || "상자"` fallback으로 재확인).

그런데 **AI_First_Step 저장소의 `docs/엔진_경계.md` §7에는 이 4건에 대한 기록이 전혀 없다.** AI_First_Step §7은 "소급 기록 안내" 주석과 함께 2026-07-22 이후의 §4 공유규범 관련 3개 항목만 담고 있고, 그 이전(2026-07-21)의 SITE_CONFIG 백포트 건은 소급 대상에서 빠져 있다.
- **심각도**: 중간(이력 추적성 결손 — 본진엔 있으니 실무 영향은 없으나, "출처 저장소" 자신의 문서에 자기 발견 이력이 없는 비대칭)
- **수용 가능 여부**: 애매 — 즉시 위험은 없음(본진이 정본 역할)이지만, §5/§7의 취지("어느 쪽에서 먼저 고쳤는지·상대에 언제 반영했는지 이력 남기기")를 완전히 만족하지 못함. 보완 권장.

### 발견 5 — 동일 이력 항목의 서술 상세도 차이(경미)
§7의 2026-07-22 "톤앤매너 헌법에서 현황 스냅샷 분리" 항목에서, 본진 쪽 기록은 "§1에 기본판/경험판 규격 분리 규칙 추가"와 "본진 콘텐츠_현황.md는 js/data.js·assets/를 직접 조회해 실측"이라는 본진 전용 세부사항을 포함하는 반면, AI_First_Step 쪽 기록은 이 세부사항 없이 더 간략하다. 이는 톤앤매너.md 자체가 §4 예외(사이트별 조정 허용) 대상이라 내용 차이가 나는 것은 정상이지만, 이력 기록에서 "두 저장소에 같은 시점에 같은 구조로 반영"이라는 문구가 이 본진 전용 추가분까지 뭉뚱그리고 있어 약간의 서술 모호함이 있다.
- **심각도**: 낮음
- **수용 가능 여부**: 수용 가능 — 실질 위반 없음, 문구 정밀화만 권장.

**"상대 저장소 동기화 필요 — 확인 못함"으로 남길 항목**: 없음(두 문서 모두 직접 열람·대조 가능했음).

---

## 종합 판정

| 발견 | 심각도 | 수용 가능 여부 |
|---|---|---|
| 1. data.js 최상위 스키마=엔진 계약 미문서화 | 낮음~중간 | 수용 가능(문서 보강 권장) |
| 2. kicker "상자" 텍스트 하드코딩 (신규 애매 항목) | 낮음 | 수용 가능(§3 등재 권장) |
| 3. box.accent = 스킨과 별개의 색상 축 | 낮음(정보성) | 수용 가능 |
| 관찰. assets 파일명 컨벤션 혼재 | 낮음(정보성) | 수용 가능, 경계 이슈 아님 |
| 4. AI_First_Step §7에 SITE_CONFIG 4건 백포트 이력 누락 | 중간 | 애매 — 보완 권장 |
| 5. §7 이력 서술 상세도 비대칭 | 낮음 | 수용 가능 |

**경계 침범**: 없음. **UX_원칙.md**: 완전 동기화. **엔진_경계.md**: 구조·원칙 동일, §7 이력 항목 하나가 출처 저장소 쪽에 누락.

---

## 이식 이력 기록 제안 문안(초안)

**(a) `엔진_경계.md` §3에 신규 애매 항목 추가 제안**:
> - **상자1 등 슬라이드 `kicker`의 "상자 N" 리터럴 텍스트**: `js/room.js`의 상자 라벨은 `SITE_CONFIG.boxLabel` fallback으로 이미 사이트별 커스터마이즈 가능하지만(§7 2026-07-21 이식), `js/data.js` 슬라이드 콘텐츠의 `kicker` 필드(예: `'상자 1 · 왜 · 01'`)는 각 강마다 문자열로 박혀 있어 `boxLabel`을 바꿔도 자동 반영되지 않는다. `data.js`는 전량 교체 대상(§2)이라 복제 시 자연히 재작성되므로 당장 문제는 아니나, 복제 작업 시점에 "라벨을 바꿀 거면 kicker도 강별로 다시 써야 한다"는 점을 인지해야 한다.

**(b) `엔진_경계.md` §2에 스키마 계약 명시 제안**:
> `js/data.js`의 콘텐츠(값)는 전량 교체 대상이지만, 최상위 키 형태(`CURRICULUM.boxes[].id/name/theme/accent`, `lectures[].id/title/tagline/slides[].type`)는 `js/room.js`·`js/main.js`·`js/slides.js`·`js/admin.js`가 직접 소비하는 사실상의 엔진 계약이다 — 복제 시 값은 바꾸되 구조는 유지해야 한다(`tests/content-lint.test.js`의 `VALID_SLIDE_TYPES`/`LECTURE_COUNT`가 이를 일부 검증).

**(c) AI_First_Step `docs/엔진_경계.md` §7에 소급 추가 제안**(그 저장소 문서 개정 시):
> ### 2026-07-21 — AI_First_Step → 본진, SITE_CONFIG fallback 4건 (소급 기록)
> 이 저장소 `de5d5a6`("첫 출근" 복제 작업)에서 발견해 본진(AX_Lecture) `js/main.js`·`js/room.js`·`js/intro.js`·`css/style.css`에 반영한 4건(스킨/칭호/완주문구/상자라벨/인트로기본테마 fallback + `.pdoor` `[hidden]` 가드 버그픽스) — 상세 내용은 본진 `docs/엔진_경계.md` §7 해당 항목 참고. 이 저장소 쪽엔 원 발견 시점(작업 커밋 로그)만 소급 기재.

이식 실행(위 문안을 실제로 문서에 반영하는 것) 여부·시점은 사용자가 결정한다.
